/*
 * DocStudio Api Documentation
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: R120.9
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.docstudio.client.model;

import java.util.Objects;
import com.docstudio.client.model.ExchangeAs2Props;
import com.docstudio.client.model.ExchangeAs4DbnaProps;
import com.docstudio.client.model.ExchangeSshProps;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.UUID;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.docstudio.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-12-13T18:50:48.300154-07:00[America/Edmonton]", comments = "Generator version: 7.10.0")
public class ExchangeProps extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(ExchangeProps.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ExchangeProps.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ExchangeProps' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ExchangeSshProps> adapterExchangeSshProps = gson.getDelegateAdapter(this, TypeToken.get(ExchangeSshProps.class));
            final TypeAdapter<ExchangeAs4DbnaProps> adapterExchangeAs4DbnaProps = gson.getDelegateAdapter(this, TypeToken.get(ExchangeAs4DbnaProps.class));
            final TypeAdapter<ExchangeAs2Props> adapterExchangeAs2Props = gson.getDelegateAdapter(this, TypeToken.get(ExchangeAs2Props.class));

            return (TypeAdapter<T>) new TypeAdapter<ExchangeProps>() {
                @Override
                public void write(JsonWriter out, ExchangeProps value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `ExchangeSshProps`
                    if (value.getActualInstance() instanceof ExchangeSshProps) {
                        JsonElement element = adapterExchangeSshProps.toJsonTree((ExchangeSshProps)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `ExchangeAs4DbnaProps`
                    if (value.getActualInstance() instanceof ExchangeAs4DbnaProps) {
                        JsonElement element = adapterExchangeAs4DbnaProps.toJsonTree((ExchangeAs4DbnaProps)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `ExchangeAs2Props`
                    if (value.getActualInstance() instanceof ExchangeAs2Props) {
                        JsonElement element = adapterExchangeAs2Props.toJsonTree((ExchangeAs2Props)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: ExchangeAs2Props, ExchangeAs4DbnaProps, ExchangeSshProps");
                }

                @Override
                public ExchangeProps read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize ExchangeSshProps
                    try {
                        // validate the JSON object to see if any exception is thrown
                        ExchangeSshProps.validateJsonElement(jsonElement);
                        actualAdapter = adapterExchangeSshProps;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'ExchangeSshProps'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for ExchangeSshProps failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'ExchangeSshProps'", e);
                    }
                    // deserialize ExchangeAs4DbnaProps
                    try {
                        // validate the JSON object to see if any exception is thrown
                        ExchangeAs4DbnaProps.validateJsonElement(jsonElement);
                        actualAdapter = adapterExchangeAs4DbnaProps;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'ExchangeAs4DbnaProps'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for ExchangeAs4DbnaProps failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'ExchangeAs4DbnaProps'", e);
                    }
                    // deserialize ExchangeAs2Props
                    try {
                        // validate the JSON object to see if any exception is thrown
                        ExchangeAs2Props.validateJsonElement(jsonElement);
                        actualAdapter = adapterExchangeAs2Props;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'ExchangeAs2Props'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for ExchangeAs2Props failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'ExchangeAs2Props'", e);
                    }

                    if (match == 1) {
                        ExchangeProps ret = new ExchangeProps();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for ExchangeProps: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public ExchangeProps() {
        super("oneOf", Boolean.FALSE);
    }

    public ExchangeProps(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("ExchangeSshProps", ExchangeSshProps.class);
        schemas.put("ExchangeAs4DbnaProps", ExchangeAs4DbnaProps.class);
        schemas.put("ExchangeAs2Props", ExchangeAs2Props.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return ExchangeProps.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * ExchangeAs2Props, ExchangeAs4DbnaProps, ExchangeSshProps
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof ExchangeSshProps) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof ExchangeAs4DbnaProps) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof ExchangeAs2Props) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be ExchangeAs2Props, ExchangeAs4DbnaProps, ExchangeSshProps");
    }

    /**
     * Get the actual instance, which can be the following:
     * ExchangeAs2Props, ExchangeAs4DbnaProps, ExchangeSshProps
     *
     * @return The actual instance (ExchangeAs2Props, ExchangeAs4DbnaProps, ExchangeSshProps)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `ExchangeSshProps`. If the actual instance is not `ExchangeSshProps`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ExchangeSshProps`
     * @throws ClassCastException if the instance is not `ExchangeSshProps`
     */
    public ExchangeSshProps getExchangeSshProps() throws ClassCastException {
        return (ExchangeSshProps)super.getActualInstance();
    }

    /**
     * Get the actual instance of `ExchangeAs4DbnaProps`. If the actual instance is not `ExchangeAs4DbnaProps`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ExchangeAs4DbnaProps`
     * @throws ClassCastException if the instance is not `ExchangeAs4DbnaProps`
     */
    public ExchangeAs4DbnaProps getExchangeAs4DbnaProps() throws ClassCastException {
        return (ExchangeAs4DbnaProps)super.getActualInstance();
    }

    /**
     * Get the actual instance of `ExchangeAs2Props`. If the actual instance is not `ExchangeAs2Props`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ExchangeAs2Props`
     * @throws ClassCastException if the instance is not `ExchangeAs2Props`
     */
    public ExchangeAs2Props getExchangeAs2Props() throws ClassCastException {
        return (ExchangeAs2Props)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ExchangeProps
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with ExchangeSshProps
        try {
            ExchangeSshProps.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for ExchangeSshProps failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with ExchangeAs4DbnaProps
        try {
            ExchangeAs4DbnaProps.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for ExchangeAs4DbnaProps failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with ExchangeAs2Props
        try {
            ExchangeAs2Props.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for ExchangeAs2Props failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for ExchangeProps with oneOf schemas: ExchangeAs2Props, ExchangeAs4DbnaProps, ExchangeSshProps. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of ExchangeProps given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ExchangeProps
     * @throws IOException if the JSON string is invalid with respect to ExchangeProps
     */
    public static ExchangeProps fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ExchangeProps.class);
    }

    /**
     * Convert an instance of ExchangeProps to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

